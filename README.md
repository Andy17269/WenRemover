# WenRemover for macOS

English | [简体中文](README.zh-CN.md)

A SwiftUI macOS utility for batch-removing EXIF metadata from images.

## Features
- Drag and drop images into the app window
- Choose an output folder
- One-click batch removal of EXIF (by re-encoding and writing out)

## Run
1. Open EXIFRemover.xcodeproj in Xcode
2. Select the “My Mac” target
3. Run

## Usage
- Drag images into the window
- Click “Choose Output Folder”
- Click “Remove EXIF”

Output files are saved to the selected folder with a “_clean” suffix.
